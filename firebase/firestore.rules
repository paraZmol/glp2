rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // regla por defecto deny todo para empezar seguro
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
